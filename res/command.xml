<category>
  <rule id="commandstop">
    <case>
			<condition operator="and">
        <if relation="=">stop</if>
        <if relation="=">halt</if>
        <if relation="=">abort</if>
        <if relation="=">kill</if>
        <if relation="=">panic</if>
        <if relation="=">off</if>
        <if relation="=">freeze</if>
        <if relation="=">shut down</if>
        <if relation="=">turn off</if>
        <if relation="=">help</if>
        <if relation="=">help me</if>
        <if relation="=">abhor</if>
        <if relation="=">a whore</if>
        <if relation="=">a bore</if>
			</condition>
			<effect>
				<then util="random">Ah.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandslowdown">
    <case>
			<condition operator="or">
        <if relation="in">you * slow down</if>
        <if relation="in">you * speak * slower</if>
			</condition>
			<effect>
				<then util="random">Well.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandspeedup">
    <case>
			<condition operator="or">
        <if relation="in">you * speed up</if>
        <if relation="in">you * speak * faster</if>
			</condition>
			<effect>
				<then util="random">Well.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandmoveforward">
    <case>
			<condition operator="and">
        <or>
          <if relation="in">move</if>
          <if relation="in">go</if>
        </or>
        <or>
          <if relation="in">forward</if>
          <if relation="in">ahead</if>
          <if relation="in">straight</if>
          <if relation="in">forwards</if>
        </or>
			</condition>
			<effect>
				<then util="random">OK moving forward.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandmovebackward">
    <case>
			<condition operator="and">
        <or>
          <if relation="in">move</if>
          <if relation="in">go</if>
        </or>
        <or>
          <if relation="in">back</if>
          <if relation="in">backward</if>
          <if relation="in">back up</if>
          <if relation="in">backwards</if>
        </or>
			</condition>
			<effect>
				<then util="random">OK moving backward.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandturnleft">
    <case>
			<condition operator="or">
        <if relation="in">move * left</if>
        <if relation="in">go * left</if>
        <if relation="in">turn * left</if>
			</condition>
			<effect>
				<then util="random">OK turning left.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandturnright">
    <case>
			<condition operator="or">
        <if relation="in">move * right</if>
        <if relation="in">go * right</if>
        <if relation="in">turn * right</if>
        <if relation="in">rotate * right</if>
			</condition>
			<effect>
				<then util="random">OK turning right.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandrotateleft">
    <case>
			<condition operator="or">
        <if relation="in">rotate * left</if>
			</condition>
			<effect>
				<then util="random">OK rotating left.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandrotateright">
    <case>
			<condition operator="or">
        <if relation="in">rotate * right</if>
			</condition>
			<effect>
				<then util="random">OK rotating right.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandquarterspeed">
    <case>
			<condition operator="or">
        <if relation="in">quarter speed</if>
        <if relation="in">quarters speed</if>
			</condition>
			<effect>
				<then util="random"></then>
			</effect>
    </case>
  </rule>
  <rule id="commandhalfspeed">
    <case>
			<condition operator="or">
        <if relation="in">half speed</if>
        <if relation="in">halfs speed</if>
        <if relation="in">halve speed</if>
			</condition>
			<effect>
				<then util="random"></then>
			</effect>
    </case>
  </rule>
  <rule id="commandfullspeed">
    <case>
			<condition operator="or">
        <if relation="in">full speed</if>
        <if relation="in">fulls speed</if>
			</condition>
			<effect>
				<then util="random"></then>
			</effect>
    </case>
  </rule>
  <rule id="commandstoplisten">
    <case>
			<condition operator="or">
        <if relation="in">pause listen</if>
        <if relation="in">stop listen</if>
        <if relation="in">pause listening</if>
        <if relation="in">stop listening</if>
        <if relation="in">top listen</if>
        <if relation="in">top listening</if>
        <if relation="in">topless in</if>
        <if relation="in">end listen</if>
        <if relation="in">endless in</if>
			</condition>
			<effect>
				<then util="random">Stop listening.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandstartlisten">
    <case>
			<condition operator="or">
        <if relation="in">continue listen</if>
        <if relation="in">resume listen</if>
        <if relation="in">start listen</if>
        <if relation="in">continue listening</if>
        <if relation="in">resume listening</if>
        <if relation="in">start listening</if>
        <if relation="in">dart listen</if>
        <if relation="in">art listen</if>
        <if relation="in">dart listening</if>
        <if relation="in">art listening</if>
        <if relation="in">begin listen</if>
        <if relation="in">begin listening</if>
			</condition>
			<effect>
				<then util="random">Start listening.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandstartfollow">
    <case>
			<condition operator="or">
        <if relation="in">continue follow</if>
        <if relation="in">resume follow</if>
        <if relation="in">start follow</if>
        <if relation="in">continue following</if>
        <if relation="in">resume following</if>
        <if relation="in">start following</if>
        <if relation="in">dart follow</if>
        <if relation="in">art follow</if>
        <if relation="in">dart following</if>
        <if relation="in">art following</if>
        <if relation="in">begin follow</if>
        <if relation="in">begin following</if>
			</condition>
			<effect>
				<then util="random">Start following.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandstopfollow">
    <case>
			<condition operator="or">
        <if relation="in">pause follow</if>
        <if relation="in">stop follow</if>
        <if relation="in">pause following</if>
        <if relation="in">stop following</if>
        <if relation="in">top follow</if>
        <if relation="in">top following</if>
        <if relation="in">end follow</if>
        <if relation="in">end following</if>
			</condition>
			<effect>
				<then util="random">Stop following.</then>
			</effect>
    </case>
  </rule>
  <rule id="commandstartspeech">
    <case>
			<condition operator="or">
        <if relation="in">continue speech</if>
        <if relation="in">resume speech</if>
        <if relation="in">start speech</if>
        <if relation="in">continue speaking</if>
        <if relation="in">resume speaking</if>
        <if relation="in">start speaking</if>
        <if relation="in">art speech</if>
        <if relation="in">dart speech</if>
        <if relation="in">art speaking</if>
        <if relation="in">dart speaking</if>
        <if relation="in">begin speech</if>
        <if relation="in">begin speaking</if>
			</condition>
			<effect>
				<then util="random"></then>
			</effect>
    </case>
  </rule>
  <rule id="commandstopspeech">
    <case>
			<condition operator="or">
        <if relation="in">pause speech</if>
        <if relation="in">stop speech</if>
        <if relation="in">pause speaking</if>
        <if relation="in">stop speaking</if>
        <if relation="in">top speech</if>
        <if relation="in">top speaking</if>
        <if relation="in">top speed</if>
        <if relation="in">end speech</if>
        <if relation="in">end speaking</if>
			</condition>
			<effect>
				<then util="random"></then>
			</effect>
    </case>
  </rule>
</category>
